# --- Análisis local (limpieza, evaluación, gráficas, baseline léxico) ---
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
regex>=2023.0
ftfy>=6.0            # opcional: reparación de mojibake (el código corre sin él)

# --- Solo para el notebook 02 (BERT) en Kaggle/Colab; NO se instalan en local ---
# transformers>=4.40
# torch
